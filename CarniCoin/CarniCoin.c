#include <stdio.h>
#include <stdlib.h>

int Coin_n[2] = { 1, 2 };
//동전은 하나 또는 두개를 뒤집을 수 있음.
int Carnicoin(int n) {
	int student, customer;

	scanf("firstable customer %d", &customer);

	if (customer > 2 && customer < 1) {
		fprintf(stderr, -1, "input err");
	}

	else if (customer == 1) {

	}

	else //custoemr == 2 

	

}

int main() {
	int num;
	scanf("%d", &num);
	Carnicoin(num);
}